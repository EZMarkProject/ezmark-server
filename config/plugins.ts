export default () => ({
    'users-permissions': {
        config: {
            jwt: {
                expiresIn: '7d', // Eg: 60, "45m", "10h", "2 days", "7d", "2y"
            }
        }
    }
});
